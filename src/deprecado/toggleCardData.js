export default function toggleCardData () {
    const personalData = document.getElementById("personalDataId");
    personalData.style.display = personalData.style.display === "none" ? "block" : "none";
}